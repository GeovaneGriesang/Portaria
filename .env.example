# Copy this file to .env and adjust values as needed.

# Definido em produção quando o app roda sob um subpath (ex.: movaci.com.br/portaria).
# Deixe vazio/ausente em desenvolvimento local (app roda na raiz).
NEXT_PUBLIC_BASE_PATH=
