# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env*.local

# testing / coverage
/coverage

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# python
__pycache__/
*.pyc

# claude code — local/pessoal, não compartilhar (settings.json e skills/ continuam versionados)
.claude/settings.local.json

# PDFs reais dos boletins — pesados (centenas de MB), sincronizados direto pro
# servidor no deploy, não versionados aqui. Os metadados extraídos (nome,
# tipo, unidade, vigência etc.) ficam em data/portarias/*.json, esses sim
# versionados.
/public/portarias/
